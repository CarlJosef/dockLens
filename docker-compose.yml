services:
  api:
    build:
      context: ./api
    ports:
      - "8000:8000"

  web:
    build:
      context: ./web
    environment:
      - VITE_API_BASE=http://api:8000
    ports:
      - "5174:80"
    depends_on:
      - api
