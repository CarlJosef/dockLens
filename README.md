# DocLens

- Windows + Docker, VS Code-first.

## Activate python enviroment before

**.\activare.ps1**

## Run and start the whole stack at vance?

```Open powershell in VScode and run :

docker compose up --build
```

Stop it by write :

docker compose down

# DocLens

Windows + Docker, VS Code-first.

## Run

```powershell
docker compose up --build
```
